# Progress

See `docs/PLAN.md` for phase descriptions, `docs/DATA_MODEL.md` for schema, `docs/API.md` for endpoint contracts.

- [x] Planning docs (PLAN.md, DATA_MODEL.md, API.md, this file)
- [x] Go toolchain installed (Go 1.26.5, user-local at `~/go-sdk`)
- [x] Phase 1 — Scaffolding
- [x] Phase 2 — Schema & migrations
- [x] Phase 3 — Auth (OTP + JWT + RBAC)
- [x] Phase 4 — Washing point CRUD
- [x] Phase 5 — Service + price option CRUD
- [x] Phase 6 — Car CRUD
- [x] Phase 7 — Availability endpoint
- [x] Phase 8 — Queue booking (create/cancel/status/list)
- [x] Phase 9 — History endpoint
- [x] Phase 10 — Notifications
- [x] Phase 11 — Hardening (errors, validation, pagination, seed, tests, README)
- [x] Swagger/OpenAPI documentation (requested after Phase 11, not part of the original 12-phase plan)
- [x] Mobile app support: client-chosen box, `cars_ahead` position, SSE live updates (requested for the `q-wash` Flutter app, not part of the original 12-phase plan)
- [ ] Phase 12 — Future/optional (Dockerfile, multi-point, per-weekday hours, push)
- [x] `docs/PLAN_WEB_APPS.md` phase 1 — Migrations for the 4 new staff/ops web apps (`q-wash-admin`/`-cabinet`/`-worker`/`-display`)
- [x] `docs/PLAN_WEB_APPS.md` phase 2 — Owners + connection requests CRUD + approval flow
- [x] IDOR/broken-access-control review + fix — see log below
- [x] `docs/PLAN_WEB_APPS.md` phase 3 — Admin washing-point extensions (owner_id/status/description/amenities, `GET /admin/washing-points`, `GET /admin/stats`, `GET /queue` network-wide filter)
- [x] `docs/PLAN_WEB_APPS.md` phase 4 — Photos (`internal/platform/storage`, `internal/photo` CRUD)
- [x] `docs/PLAN_WEB_APPS.md` phase 5 — Per-weekday schedule (`internal/schedule` CRUD + availability/booking-creation rework)
- [x] `docs/PLAN_WEB_APPS.md` phase 6 — Boxes (`internal/box` CRUD + availability/queue-create closed-box filter)
- [x] `docs/PLAN_WEB_APPS.md` phase 7 — Worker role (`requireQueueOps`, pause/resume, broadened cancel, live-boxes, queue date filter)
- [x] `docs/PLAN_WEB_APPS.md` phase 8 — Display board (`GET /washing-points/{id}/board`, staff/admin only, no SSE variant)
- [ ] `docs/PLAN_WEB_APPS.md` phase 9 (docs+tests batch — partially covered incrementally per-phase already, see log)

## Log

- 2026-08-05 — Clarified requirements (boxes/capacity, roles, SMS stub, price sub-types, DB/router choice, operating hours). Wrote planning docs. Installed Go 1.26.5 locally.
- 2026-08-05 — Switched primary keys to UUIDv7 per feedback. Installed Docker via apt (sudo). Phase 1 done: `go.mod` initialized (module `q-wash-api`), full folder structure, chi router with `/health`, GORM+Postgres connection, config loader (env + `.env`), graceful-shutdown `main.go`, `docker-compose.yml` (Postgres 16), `Makefile`, `.env.example`, `.gitignore`. Verified end-to-end: `docker compose up`, `go run ./cmd/api`, `GET /health` → `200 {"status":"ok"}`. Initialized git repo (`main` branch), committed Phase 1 as `5f60488`.
- 2026-08-05 — Phase 2 done: 9 golang-migrate migrations for all tables in DATA_MODEL.md (installed `migrate` CLI via `go install`). `washing_points.open_time`/`close_time` stored as validated `varchar(5)` "HH:MM" text instead of Postgres `time`, to avoid GORM/pgx type-mapping friction. Added a Postgres `EXCLUDE` constraint (`btree_gist`) on `queue` so the DB itself guarantees no two overlapping, non-canceled bookings can share the same box at the same washing point — a second line of defense beyond the app-level availability check planned for Phase 7/8. Wrote GORM model structs for every table (UUIDv7 `BeforeCreate` hooks) in each `internal/<domain>` package. Added `cmd/seed` (`make seed`): idempotent baseline dev data (1 washing point/2 boxes, 2 services with price options, admin/staff/customer users, 1 car, 2 non-overlapping bookings) plus a self-check that intentionally attempts a double-booking and confirms Postgres rejects it. Verified: migrations apply cleanly, seed runs twice with identical IDs (idempotent), constraint self-check passes.
- 2026-08-05 — Phase 3 done: `internal/apperror` + `internal/httputil` (shared error type → HTTP JSON envelope), `internal/platform/reqctx` (context-carried auth principal, dependency-free to avoid import cycles), `internal/platform/sms` (Sender interface + stdout stub), `internal/platform/jwt` (HS256 access-token issue/verify). `internal/auth`: OTP request/verify (bcrypt-hashed codes, cooldown + max-attempts), refresh-token rotation (opaque random tokens, HMAC-SHA256-hashed at rest via `REFRESH_TOKEN_PEPPER`, never JWTs so sessions are individually revocable), idempotent logout, `RequireAuth`/`RequireRole` middleware. `internal/user`: repository + `GET/PATCH /me`. `config.AuthConfig` added (access/refresh TTLs, OTP TTL/cooldown/max-attempts). `httpserver.NewRouter` now returns the `/api/v1` sub-router so `main.go`'s new `wireRoutes` can mount each feature's handlers without the router package knowing about them. Verified end-to-end against the running server: OTP request → stub-logged code → verify → user auto-created (role `customer`) → `GET/PATCH /me` (401 without token, 200 with) → refresh rotates the pair and revokes the old refresh token (reuse correctly 401s) → logout (204, idempotent). Reused/expired/locked OTP paths also exercised.
- 2026-08-05 — Phase 4 done: `internal/washingpoint` repository + handler — public `GET /washing-points` (list) and `GET /washing-points/{id}`; staff/admin-only `POST`, `PATCH` (partial update, re-validates the merged record), `DELETE` (deactivates via `is_active=false`, no row deletion). Added `httputil.ParseUUIDParam` as a shared path-param helper for all future CRUD phases. Hit a chi gotcha: `Route()` internally `Mount()`s, so registering the same path prefix ("/washing-points") from two separate `Group()`s panics at startup ("attempting to Mount() a handler on an existing path") — fixed by registering all methods for a resource in one `Route()` call and applying the staff/admin middleware per-method via chi's `With(...)`; this is now the pattern for every future resource handler. Verified end-to-end against the running server: public list/get work with no token; create/update/delete correctly 401 with no token, 403 for a logged-in customer, and succeed for staff; validation errors (e.g. `close_time` before `open_time`) return 400 with the expected code; deactivate flips `is_active` and a bad id 404s.
- 2026-08-05 — Phase 5 done: `internal/service` repository + `Manager` (business rules) + handler. Public `GET /washing-points/{id}/services` (nested, includes price options) and `GET /services/{id}`; staff/admin `POST /washing-points/{id}/services` (requires a non-empty `price_options` array, auto-promotes the first to default if none marked, rejects >1 marked default), `PATCH /services/{id}` (service's own fields only — `Repository.Update` uses an explicit column map instead of `Save()` so it never cascades into the `PriceOptions` association), `DELETE /services/{id}` (deactivate). Price options: `POST /services/{id}/price-options`, `PATCH /price-options/{id}` (rejects explicitly unsetting the current sole default — 400 `cannot_unset_default`), `DELETE /price-options/{id}` (409 if it's the last option or referenced by any `queue` row — checked directly against the `queue` table by name rather than importing the `queue` package, avoiding a future import cycle; auto-promotes another remaining option to default if the deleted one was the default). Verified end-to-end against the running server: nested price options load correctly on list; `price_options_required` / `multiple_default_price_options` on create; default auto-promotion on create when none specified; `cannot_unset_default` when unsetting the sole default; default reassignment via PATCH then successful delete of the now-non-default option; `last_price_option` 409 on deleting the only remaining option; `price_option_in_use` 409 against a real seeded booking; auto-promotion of a new default after deleting the previous default; 403 for a customer trying to create a service. Seeder re-run confirmed no regressions.
- 2026-08-05 — Phase 6 done: `internal/car` repository + handler. `GET/POST /me/cars` and `PATCH/DELETE /cars/{id}` require only authentication (any role) — ownership, not role, is what's enforced: `FindOwnedByID` scopes by `id AND user_id` together so a non-owner's request 404s exactly like a nonexistent id (never 403), avoiding leaking whether a given car id exists. `DELETE` checks the `queue` table directly (by name, same pattern as the service package's price-option guard) and returns 409 `car_in_use` if any booking references the car; cars are hard-deleted otherwise (no `is_active` flag, unlike washing points/services). Verified end-to-end: 401 unauthenticated; list/create/patch work for the owner; a second logged-in user (staff) gets 404 patching the customer's car; delete is blocked 409 on the seeded car with a real booking, succeeds on an unused one. Seeder re-run confirmed no regressions.
- 2026-08-05 — Phase 7 done: `GET /washing-points/{id}/availability`, first piece of the `internal/queue` package (repository + algorithm + handler; booking CRUD itself is Phase 8). `queue.ComputeAvailableSlots` (`internal/queue/availability.go`) is a pure, DB-free sweep-line function — generates candidate `[start, start+duration)` windows every 15 minutes across `open_time`–`close_time`, keeping ones where concurrent busy-box count never reaches `boxes_count`; unit tested in `availability_test.go` (no bookings, single box fully booked, the exact seeded double-booking scenario, back-to-back non-overlap at the boundary, duration-doesn't-fit, invalid inputs) — all pass. `queue.Repository.FindActiveBookingsInRange` fetches the busy intervals; no cross-midnight handling is needed since `close_time > open_time` is enforced at write time, so every booking is contained within one calendar day by construction. Handler validates `service_id`/`date` format, 404s on unknown washing point/service, 400s if the service belongs to a different washing point, and treats all scheduling as UTC (no timezone field yet, single-point deployment — noted in both docs). Verified end-to-end against the running server: results around the seeded box1 10:00–11:30 / box2 10:30–11:00 booking exactly matched hand-computed expectations (the 10:15–11:00 range disappears since both boxes would be occupied, single-box-busy slots on either side remain available); validation and 404 paths also exercised. Full `go test ./...` and seeder re-run both clean.
- 2026-08-05 — Phase 8 done: `queue.Manager` (`internal/queue/manager.go`) completes the `internal/queue` package. `POST /queue` validates car ownership, service/price-option/washing-point relationships, that `scheduled_start_at` is in the future and within operating hours, then assigns a box and inserts inside a transaction that takes `SELECT ... FOR UPDATE` on the washing point row — serializing concurrent booking attempts for that washing point (locking the individual overlapping `queue` rows wouldn't help, since a currently-free box may have no existing row to lock). `Repository.Create` also translates a Postgres `23P01` exclusion-violation (the `queue_no_overlapping_box_bookings` constraint from Phase 2) into the same `slot_unavailable` 409, as a backstop if that lock were ever bypassed. `PATCH /queue/{id}/cancel` (owner-only, only from `queue`/`waiting`) and `PATCH /queue/{id}/status` (staff/admin, forward-only one step at a time via a `map[Status]Status`, canceled/ready are terminal) round out the state machine; `GET /queue/{id}` is owner-or-staff (404 for anyone else, not 403); `GET /washing-points/{id}/queue` is the staff live-queue board. Booking creation is intentionally not role-restricted to "customer" — any authenticated user can book for themselves, matching the car package's ownership-over-role pattern. Route registration follows the established single-`Route()`-plus-per-method-`With()` pattern across three prefixes (`/washing-points/{id}/availability`, `/washing-points/{id}/queue`, `/queue`) mounted alongside washingpoint's and service's own `/washing-points...` routes with no conflict, confirming that pattern generalizes. Verified end-to-end against the running server: full booking flow including box-1-then-box-2 assignment for two overlapping requests, `slot_unavailable` on a third; every validation path (past time, outside hours, mismatched price option, someone else's car) 400/404s correctly; RBAC on status transition (403 for customer) and the forward-only state machine (skip and backward moves both 409, terminal states protected); cancel then successful rebooking of the freed box; staff board shows exactly the live bookings. Full `go test ./...` and seeder re-run both clean.
- 2026-08-05 — Phase 9 done: `GET /me/queue`, plus generic pagination helpers added to `httputil` (`ParsePagination`, `Pagination.Offset/Limit`, `WritePaginated[T]` — Go generics) for reuse by any future list endpoint. `queue.Repository.ListByUser` returns the caller's bookings across all statuses, most-recently-scheduled first, with a `Count` query for `total`. Invalid/out-of-range `page`/`page_size` clamp to defaults (`page=1`, `page_size=20`, max `100`) rather than erroring — pagination params are a convenience, not worth rejecting a request over. Route registered as a fourth distinct `Route()` prefix (`/me/queue`) in `queue.Handler.RegisterRoutes`, confirmed not to conflict with `user.Handler`'s literal `/me` or `car.Handler`'s `/me/cars` mount. Verified end-to-end: 401 unauthenticated; correct 2/2/1 split across 3 pages of `page_size=2` over 5 real bookings (caught and fixed a test-script mistake first — booking dates in the past are correctly rejected by Phase 8's future-only validation, exactly as intended); malformed `page=abc&page_size=99999` clamps to `page=1, page_size=100` instead of erroring; a second user's `/me/queue` correctly shows zero of the first user's bookings. Full `go test ./...` and seeder re-run both clean.
- 2026-08-05 — Phase 10 done: `internal/notification` (repository + `Manager` + handler) completes the queue-adjacent feature set. `POST /notifications` (staff/admin) validates `user_id` (404 `user_not_found`) and, if given, that `queue_id` both exists and belongs to that same `user_id` (400 `queue_user_mismatch` otherwise — caught via `queueRepo.FindByID`, no import cycle since `queue` doesn't import `notification`). If `send_at` is due (omitted, or now/past), the manager immediately calls the same `sms.Sender` used for OTP delivery and persists the outcome (`status: sent`/`failed`, `sent_at` set); a future `send_at` just leaves the row `pending` — there's still no background worker to pick it up later (noted as future work in PLAN.md Phase 12 and DATA_MODEL.md). The HTTP call always succeeds once the row is created regardless of delivery outcome, since delivery result is a property of the record, not the request. `GET /me/notifications` reuses Phase 9's pagination helpers verbatim. Verified end-to-end against the running server: 403 for a customer posting, 201 with immediate `sent` status and a matching stub-SMS log line for a due notification, `pending` status (no send attempt) for a `send_at` in December, 404/400 on bad `user_id`/empty text, `queue_user_mismatch` on a deliberately mismatched `queue_id` then 201 on a correctly matched one, `GET /me/notifications` showing exactly the 3 records for the right user and 0 for an unrelated staff account, 401 unauthenticated. Full `go test ./...` and seeder re-run both clean.
- 2026-08-05 — Phase 11 done (hardening). **Consistency audit**: found and fixed a real bug — `POST /auth/refresh` used the same code `invalid_refresh_token` for both a missing field (400) and an actually-invalid token (401), so a client couldn't distinguish them; the 400 case is now `refresh_token_required`. Also found `service` create/update had no length caps on `description`/`picture_url` (every other free-text field in the API has one) — added `invalid_description` (2000) and `invalid_picture_url` (500), matching the DB column sizes. **Refactor**: extracted `cmd/api/main.go`'s router/handler wiring into `internal/app.New(db, cfg, smsSender)` — `cmd/api` is `package main` and can't be imported, so integration tests need this to exist as an importable package to exercise the exact same server. Made the SMS sender an explicit parameter of `app.New` (was hardcoded to the stub inside it) specifically so tests can inject a spy that captures OTP codes instead of only logging them. **Unit tests**: added `internal/queue/manager_test.go` for the two pure pieces of `Manager` — `assignBox` (lowest-free-box selection, all-occupied, non-overlapping bookings don't block, the same back-to-back boundary case as the availability tests) and `forwardStatusTransitions` (exact transition map, terminal states have no entry). **Integration tests**: `internal/integration/` (build tag `integration`, so plain `go test ./...` stays fast and Docker-free) spins up a real, ephemeral Postgres per test via testcontainers-go, runs every migration through golang-migrate's Go library, and drives `app.New`'s exact HTTP wiring via `httptest.Server` — real requests over real HTTP, not direct function calls. Four test functions: full auth flow (OTP → tokens → consumed-code-rejected → refresh rotation with old-token-rejected → idempotent logout), RBAC on washing point management, double-booking prevention + cancel-then-rebook (boxes_count=1 to force the conflict in two requests), and the forward-only status state machine (skip rejected, terminal state protected). Since there's no API to promote a user to staff/admin (by design — see DATA_MODEL.md), the test harness does it via direct DB access after first login, same as a real deployment would. All fixture data (washing points, services, cars) is created through the real HTTP API as staff, not seeded directly, so the tests exercise genuine request paths throughout. `make test-integration` added; confirmed zero leftover containers after a full run. **README.md** added: quick start, Makefile targets, test tiers, project layout, config, and known limitations. Final full check: `gofmt`, `go vet` (both with and without the integration tag), `go build`, `go test ./...`, `go test -tags=integration ./...`, and a live smoke test against the dev docker-compose Postgres all clean.
- 2026-08-05 — Swagger/OpenAPI docs added (user request, outside the original 12-phase plan). Hand-authored `docs/openapi.yaml` (OpenAPI 3.0.3, 23 paths / 33 operations / 30 schemas) rather than code-annotation generation (e.g. swaggo) — every request/response DTO was cross-checked directly against the actual handler structs in each package (not just against `docs/API.md`'s prose) to make sure the spec matches real behavior, not just documented intent. Validated with `@redocly/cli lint` (via `npx`): structurally valid, only style warnings (missing tag descriptions, since fixed; a few genuinely-parameterless public GETs flagged for "no 4xx", left as-is since they truly have none). `docs/docs.go` (`package docs`, `//go:embed openapi.yaml`) embeds it so serving doesn't depend on the working directory at runtime — go:embed can't reach outside its own package directory, so the embed lives alongside the spec in `docs/` rather than being duplicated into an `internal/` package. `internal/platform/httpserver` gained `GET /openapi.yaml` (raw spec) and `GET /docs` (Swagger UI — swagger-ui-dist pulled from a CDN, not vendored, to avoid embedding several MB of JS/CSS for a docs page; "Try it out" requests still go same-origin to this API). Both routes are public, alongside `/health`. Verified: embedded spec byte-identical to the source file via `diff` against a live request; rendered `/docs` in headless Chrome and visually confirmed correct tag grouping, HTTP-method color coding, and auth lock icons on every protected operation. Updated `docs/API.md` and `README.md` to point at it.
- 2026-08-05 — Added CORS support (`CORS_ALLOWED_ORIGINS`, default `*`, via `github.com/go-chi/cors`) so a separately-hosted frontend (different origin/port) can call this API from a browser — needed for two new sibling projects, `../pegasus-frontend` (a full interactive test-flow UI: login, browse, book, cancel, staff panel) and `../pegasus-board` (a very simple, big-font, auto-refreshing TV-style live queue display, styled like a bank-office queue screen). Both are plain HTML/CSS/JS, no build step, and were verified end-to-end against this API using the `claude-in-chrome` browser automation skill (real clicks, not just curl) — full auth flow, booking, cancel, car CRUD, staff washing-point/service creation, and live status-transition auto-refresh all confirmed working with zero console errors. Neither sibling project is a Go module or tracked in this repo's history.
- 2026-08-05 — Enriched `GET /washing-points/{id}/queue` (the staff board endpoint) for `pegasus-board`'s new "current and upcoming queue" list view: added `customer_phone_last4` and `car_name` to a new, display-oriented `BoardItem` response shape (distinct from the full `Booking` object — deliberately omits `user_id`/`car_id` and never exposes a full phone number, since this endpoint is meant to back a shared/public-facing screen). Added `user.Repository.FindByIDs` and `car.Repository.FindByIDs` (batch fetch by id, no N+1) and a `queue.Handler.toBoardItems` enrichment step that builds phone/car lookup maps from one batched query each. Caught and fixed a real YAML bug while updating `docs/openapi.yaml`: an unquoted flow-style string containing a comma silently corrupted the spec (YAML flow mappings treat `,` as a field separator) — re-validated clean with `@redocly/cli lint` after quoting it, and removed the now-unused `BookingList` schema. Verified live: the enriched response returns exactly `{customer_phone_last4: "0003", car_name: "Demo Car", ...}` for the seeded booking, matching `+15550000003`'s real last 4 digits with no other PII present. Full `go test ./...` and `go test -tags=integration ./...` clean.
- 2026-08-05 — Added username/password login (`POST /auth/login`) for staff/admin surfaces (`pegasus-board`, and `pegasus-frontend`'s staff panel), replacing phone+OTP there; customers keep phone+OTP unchanged. Discussed schema placement with the user first: username/password_hash live as nullable columns directly on `users` (migration `000010_add_user_credentials`) rather than a separate `staff_credentials` table — staff/admin already need a full `User` row regardless (they can book, appear in `queue.user_id`, receive notifications), so a separate table would only add a join everywhere a staff identity is resolved, no real normalization win at this scale; decision recorded in `docs/DATA_MODEL.md`. `auth.Service.LoginWithPassword` returns the identical generic 401 `invalid_credentials` for both a wrong password and an unknown username (no enumeration), and 403 `forbidden` if the credentials are correct but the role isn't staff/admin — verified both paths live and in a new `TestPasswordLogin` integration test (5 subtests: success + token works on a staff-gated route, wrong password, unknown username, non-staff role rejection, missing fields → 400 not 401). No API endpoint to *set* credentials, by design, same as role promotion — `cmd/seed` sets dev-only `admin`/`admin12345` and `staff`/`staff12345` directly via a new `user.Repository.SetCredentials`. Full `go test ./...` and `go test -tags=integration ./...` clean; `docs/openapi.yaml` re-validated with `@redocly/cli lint`.
- 2026-08-18 — Extended the API for the new `q-wash` Flutter mobile app (in `../q-wash`): (1) `GET /washing-points/{id}/availability` now reports `available_boxes` per slot via a new pure function `queue.ComputeAvailableSlotsWithBoxes` (the old aggregate `ComputeAvailableSlots` is kept, still unit tested, just no longer called by the handler); (2) `POST /queue` takes a required client-chosen `box_number` instead of auto-assigning one — `Manager.assignBox` was replaced by `Manager.verifyBoxAvailable` (range-checked against `boxes_count` before the transaction, occupancy-checked against the freshly re-read busy set inside it, same `slot_unavailable` 409 and same row-locked race safety as before); (3) booking responses (`create`/`get`/`cancel`/`history`/events) gained a computed `cars_ahead` int — count of other active (`queue`/`waiting`/`washing`) bookings at the same washing point scheduled earlier, via `Repository.CountActiveAhead`, deliberately never exposing whose bookings they are; (4) new `GET /queue/{id}/events` SSE endpoint (owner-only, same 404-not-403 ownership pattern as `GET /queue/{id}`) pushing a fresh snapshot on every relevant change via a new `internal/platform/eventbus` in-memory pub/sub (`Manager` publishes on create/cancel/status-update), plus a 25s keep-alive ping, closing after a terminal status. Had to special-case the router's global `middleware.Timeout(30s)` (`skipTimeoutForSSE` in `httpserver/router.go`) — its wrapped `ResponseWriter` doesn't implement `http.Flusher` at all, so a streaming handler needs to bypass it outright, not just get a longer duration; matched on a path suffix of `/events`. Rewrote `manager_test.go`'s `TestAssignBox_*` table as `TestVerifyBoxAvailable_*` (behavior differs: verifying one specific box vs. picking the lowest free one) and added `box_number` to the two integration-test booking bodies in `internal/integration/scenarios_test.go` that were relying on auto-assignment. Updated `docs/API.md`, `docs/openapi.yaml` (re-validated with `npx @redocly/cli lint` — clean, only 4 pre-existing unrelated warnings), `docs/DATA_MODEL.md`, and `README.md`'s known-limitations section (SSE has no persistence/multi-instance story yet). `go build ./...`, `go vet ./...`, and `go test ./...` all clean; `test-integration` not run this session — Docker is blocked by this environment's shell policy, noted as a gap rather than worked around.
- 2026-08-19 — Two real bugs found and fixed via live end-to-end testing of `q-wash` against a running server (see `../q-wash/PROGRESS.md` for the mobile-side testing narrative):
  1. **Scheduling timezone.** `combineDayAndTime` (`internal/queue/handler.go`) built `open_time`/`close_time` instants in `time.UTC` — so a washing point's stated "08:00–20:00" actually meant 08:00–20:00 *UTC*, which is 13:00–01:00 the next day in Tajikistan (the app's only market, `+992` numbers, `Asia/Dushanbe`, UTC+5, no DST) — confirmed live on an Android emulator set to that timezone: the booking screen offered slots from 13:00 to 00:30. Fixed with a package-level `businessLocation` (`time.LoadLocation("Asia/Dushanbe")`, `_ "time/tzdata"` blank-imported so it doesn't depend on host zoneinfo files); `day` is converted into that location before its Y/M/D are read, so a UTC instant near local midnight still resolves to the correct local calendar day. Updated `docs/DATA_MODEL.md`/`docs/API.md` (previously documented as "treated as UTC" — corrected). No migration needed (behavioral only); existing `futureBookingTime` integration-test helper (10:00 UTC, comment says "within 08:00-20:00") still passes since 10:00 UTC still falls within the new 03:00–15:00 UTC window.
  2. **No one-active-booking-per-user rule.** Nothing prevented a user from creating a second (or third) simultaneous active booking — `q-wash`'s own queue screen assumes exactly one ("Моя очередь", singular) and would silently show only the first active one found in paginated history, leaving any others invisible and impossible to cancel from the app. Fixed with the same defense-in-depth pattern already used for box-overlap: an app-level fast-path check (`Repository.HasActiveForUser`, called from `Manager.CreateBooking` before the transaction) plus a DB-level backstop (`queue_one_active_booking_per_user`, a partial `UNIQUE` index on `user_id` where `status IN ('queue','waiting','washing')`, migration `000011`) for the race the row lock doesn't cover — two of the *same* user's requests landing concurrently for *different* washing points. Both paths return 409 `active_booking_exists`. Updated `TestBooking_DoubleBookingPreventionAndCancelRebook` in `internal/integration/scenarios_test.go`: the existing "second booking rejected" subtest now correctly expects `active_booking_exists` instead of `slot_unavailable` (since the new check fires first for the same customer), and a new subtest with a second customer/car covers the original box-conflict path so `slot_unavailable` coverage isn't lost. Updated `docs/API.md`/`docs/DATA_MODEL.md`/`docs/openapi.yaml`.

  Verified live: had the user restart `make run`, confirmed via `curl` that availability now returns `+05:00`-offset timestamps instead of `Z`; on the mobile side, confirmed the booking screen's time grid starts at 08:00 and the car-deletion error (a pre-existing, separately-fixed localization bug) now shows the right Russian string. `go build ./...`, `go vet ./...` (both with and without `-tags=integration`), `go test ./...`, and `npx @redocly/cli lint docs/openapi.yaml` (4 pre-existing unrelated warnings, no new ones) all clean. `test-integration` itself still not run — Docker blocked by this environment's shell policy.
- 2026-08-19 — Applying migration `000011` against the user's real dev DB surfaced a genuine data conflict: `cmd/seed`'s own demo data violated the new `queue_one_active_booking_per_user` index — its "both boxes busy" scenario had put two active bookings (box 1 and box 2) on the *same* demo customer, back when that was still a valid state. `migrate`/`psql` are both blocked by this sandbox's shell policy, so resolved it with a throwaway `cmd/cleanup` one-off (built, run once via `go run`, deleted immediately — never committed) that cancels all but the most-recently-created active booking per user; found and fixed exactly the one conflicting account (the seed customer, not session leftovers). Then fixed `cmd/seed` itself so this can't recur: the box-1/box-2 demo scenario now uses two separate demo customers (`+15550000004` for box 2, `+15550000005` reserved for the overlap-constraint self-check so that check stays a pure test of the box-exclusion constraint rather than also tripping the new per-user one) instead of double-booking one account. `golang-migrate` had also marked itself dirty at version 11 from the first, failed attempt — cleared with `migrate force 10` before retrying `migrate up`, which then applied cleanly. Verified `active_booking_exists` live end-to-end after the user restarted `make run` again: a second booking attempt while one was already active was correctly rejected, and `GET /me/queue` showed no phantom row from the rejected attempt. `go build ./...`, `go vet ./...`, `go test ./...` all clean after the `cmd/seed` changes.
- 2026-08-20 — **`docs/PLAN_WEB_APPS.md` phase 1 done** (migrations for the 4 new web apps replacing the throwaway `pegasus-frontend`/`pegasus-board` harnesses — see that doc for the full gap analysis and phases 2–9). 8 new migration pairs (`000012`–`000019`): `owners` and `connection_requests` tables (net-new, not yet wired to any handler); `boxes` (net-new, backfilled one row per existing point per box number); `washing_point_schedules` (net-new, backfilled 7 rows per point matching current `open_time`/`close_time` — purely additive, the availability algorithm still reads the old columns until phase 5 reworks it); `washing_point_photos` (net-new); `washing_points` gained `owner_id`/`status`/`description`/`amenities`/`display_token` and **lost** `is_active` (migrated into `status`); `users` gained a fourth role `worker` (dropped/re-added the `users_role_check` CHECK constraint — Postgres's default auto-generated name for an unnamed column-level CHECK, confirmed by convention since Docker/psql access to verify directly is blocked in this sandbox) and a nullable `washing_point_id` (backfilled to the one existing point for the seeded `staff` user); `queue` gained a nullable `paused_at` (not a new status value — the forward-only state machine is untouched, pause/resume will toggle this alongside `status = washing`). `pgcrypto` extension enabled for `gen_random_bytes()`/`gen_random_uuid()` use in migrations (bundled in the `postgres:16-alpine` image's contrib modules, no image change needed). Went with the `is_active` → `status` removal now rather than a dual-write period since this is pre-launch for the new apps — updated every Go reference in the same pass: `washingpoint.WashingPoint` model (added `Status` type + `pq.StringArray` for `Amenities`, promoted `github.com/lib/pq` from an indirect to a direct dependency via `go mod tidy`; `DisplayToken` auto-generated in `BeforeCreate` via `crypto/rand`, deliberately never included in `Handler`'s response since `GET /washing-points/{id}` is a public route and the token is meant to gate the future unauthenticated display-board endpoint), `washingpoint.Handler`'s create/update/response DTOs (`is_active bool` → `status string`, new `invalid_status` 400), `washingpoint.Repository.Deactivate` (now sets `status = paused`), `queue.Manager.CreateBooking`'s `wp.IsActive` check, `cmd/seed`. `user.User` and `queue.Queue` also gained their new columns as dormant struct fields (`WashingPointID`, `RoleWorker`, `PausedAt`) — not surfaced by any handler yet, staged for phases 2/7. Updated `docs/DATA_MODEL.md` (new entities, revised `WashingPoint`/`User`/`Queue` tables, relationship diagram), `docs/API.md`, and `docs/openapi.yaml`'s `WashingPoint`/`WashingPointUpdate`/`User` schemas and prose for the `is_active`→`status` rename. `go build ./...`, `go vet ./...`, `gofmt -l .` (clean), `go test ./...` all clean; `npx @redocly/cli lint docs/openapi.yaml` valid (same 4 pre-existing warnings as before, no new ones). Not run: `migrate up` against a real DB and `test-integration` — Docker/`psql` still blocked by this sandbox's shell policy, same gap as every prior session; the `users_role_check` constraint-name assumption in particular should be double-checked the next time the user has this running against a real Postgres.
- 2026-08-20 — **`docs/PLAN_WEB_APPS.md` phase 2 done**: two new packages, `internal/owner` and `internal/connectionrequest`, both following the existing repository+handler (+manager for the one with real business logic) shape. `owner.Handler` (`GET/POST /owners`, `GET/PATCH /owners/{id}`) is plain CRUD. `connectionrequest.Handler` adds `GET/POST /connection-requests`, `GET /connection-requests/{id}`, and `PATCH /connection-requests/{id}` (body `{status: approved|rejected}`) — the only write on an existing request, 409 `connection_request_already_reviewed` if not still `new`. `connectionrequest.Manager.Approve` is the one real business rule: reuses an `Owner` whose `contact_phone` matches the request's (via new `owner.Repository.FindByPhone`, nil-not-error when no match) or creates one, then creates a `WashingPoint` with `status = pending_review` and placeholder `latitude`/`longitude` of `0,0` (per the user's decision this session: approval alone does *not* activate a point — an admin must finish setup and correct the coordinates via the existing `PATCH /washing-points/{id}` before it can go live; the connection-request form itself doesn't collect coordinates, that's the separate not-yet-built admin "+ New point" wizard flow). Both routers gated by a new `requireAdmin` middleware chain in `app.go` (`RequireRole(admin)` only, not staff+admin like every other management route) — reasoned explicitly in a comment: staff are scoped to one point via `user.User.WashingPointID` (added in phase 1) and have no business managing another point's owner or the onboarding queue. Updated `docs/API.md` (new "Owners & connection requests" section) and `docs/openapi.yaml` (2 new tags, 6 new path items, 8 new schemas). `go build ./...`, `go vet ./...`, `gofmt -l .`, `go test ./...` all clean; `npx @redocly/cli lint docs/openapi.yaml` valid, same 4 pre-existing warnings, no new ones. Not run: live end-to-end against a real DB (Docker/psql still blocked here) — worth exercising the approve/reject flow and the phone-match owner-reuse path specifically next time a real Postgres is available, since that logic branch has zero test coverage yet.
- 2026-08-20 — **Course-corrected `q-wash-display`'s auth design**, at the user's suggestion, before phase 8 was built: dropped the planned unauthenticated `?token=` scheme (a `display_token` column on `washing_points`, generated in `BeforeCreate` via `crypto/rand`, added in phase 1) in favor of the exact same username+password login every other app uses — it directly matches `pegasus-board`'s already-proven pattern for an unattended screen (log in once, the rotating refresh token keeps the session alive indefinitely). Since nothing had been built on top of `display_token` yet (phase 8 hadn't started), this was a clean removal rather than a migration-reversal: edited migration `000013` in place (never applied to any real DB) to drop the `display_token` column/index and the now-unneeded `pgcrypto` extension enablement (`boxes`/`washing_point_schedules`' `gen_random_uuid()` calls are core Postgres 13+, not `pgcrypto`), and removed `WashingPoint.DisplayToken` plus its `BeforeCreate` generation from the Go model. Updated `docs/PLAN_WEB_APPS.md` (the `q-wash-display` section, phase 1's bullet, phase 8's bullet, and the now-resolved open question) and `docs/DATA_MODEL.md`'s `WashingPoint` table. `go build ./...`, `go vet ./...`, `gofmt -l .` all clean.
- 2026-08-20 — **IDOR / broken-access-control review and fix**, at the user's request ("check for IDOR issues... one token won't have access to other's data"). Root cause: phase 1 added `User.WashingPointID` to scope staff/worker to one point, but nothing enforced it — every staff-gated handler checked role only, so any staff token had full read/write access to every other point's data too (harmless with the single pre-multi-point washing point that existed before this session, a real cross-tenant hole now that the admin app can create a second one). Found 7 instances plus one unrelated bug in the same auth path (the new `worker` role couldn't actually log in — `LoginWithPassword`'s allow-list wasn't updated when the role was added in phase 1). Reported via `ReportFindings` first, fixed after explicit user confirmation ("do").

  **Fix**: `WashingPointID` added to `jwt.Claims` (`wpid` claim, omitted for admin/customer) and `reqctx.AuthUser`, populated at login (`auth.Service.issueTokenPair` → `jwt.Manager.Generate`, now takes a `*string`) and read back by `auth.RequireAuth` — so every handler gets it for free via the context, no extra DB hit. New `reqctx.AuthUser.OwnsWashingPoint(id)` method (admin always true; staff/worker only if it's their own) is the one shared check every fix below calls. Applied to: `washingpoint.Handler.update`/`.deactivate` (own point only) and `.create` (now **admin-only** — staff go through the connection-request onboarding flow instead, matching what phase 2 actually built); `service.Handler.create`/`.update`/`.deactivate`/`.addPriceOption` (own point, via the service's `WashingPointID`) and `.updatePriceOption`/`.deletePriceOption` (new `checkOwnsPriceOption` helper — a price-option id alone doesn't reveal its washing point, so it resolves price-option → service → point first); `queue.Handler.get` (folded into the existing owner-or-staff check), `.updateStatus` (now loads the booking before mutating it, where it previously didn't even read the caller), `.listByWashingPoint` (the board endpoint — this was the most severe one, leaking `customer_phone_last4`/`car_name` cross-point); `notification.Manager.Create` (new `Caller` field on `CreateInput`, checked against the target `queue_id`'s washing point when one is given). Fixed the worker-login gap with a one-line allow-list addition in `LoginWithPassword`.

  **Integration test fallout**, reasoned through manually since Docker is blocked here (the tests compile either way, so `go vet` wouldn't have caught a behavioral mismatch): `testEnv.promoteToRole` only ever set `role`, never `washing_point_id`, so every existing staff-role integration scenario would now fail its own assertions once actually run (staff tokens with a nil `wpid` claim can't own anything). Fixed proactively rather than leaving known-broken (if unrun) tests: added `testEnv.setWashingPointID` + `testEnv.reLogin` (JWT claims are fixed at issuance, so a DB-side scoping change needs a fresh token to take effect — mirrors the existing "promote then re-verify" shape `loginAs` already used for role changes). `TestRBAC_WashingPointManagement` rewritten: staff now expects 403 on create (was 201), added an explicit admin-succeeds case and a new regression subtest (staff PATCHing a point they don't own → 404) so the exact vulnerability just fixed has a test against it. `bookingFixture`/`setUpBookingFixture` reworked to create the point as admin, then scope+re-login the staff account to it (returns `staffAccess` on the fixture); its two callers (`TestBooking_DoubleBookingPreventionAndCancelRebook`, `TestQueue_ForwardOnlyStatusTransitions`) updated accordingly. `TestPasswordLogin`'s existing RBAC smoke-check against a fake all-zeros washing-point id needed no change — it only asserts the response isn't 401/403, and a 404 (the new correct behavior for an unowned point) doesn't trip that.

  Updated `docs/API.md` and `docs/openapi.yaml` throughout (every affected endpoint's role/scoping prose, `createWashingPoint` now documented admin-only). `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...`, `gofmt -l .`, `go test ./...` all clean; `npx @redocly/cli lint docs/openapi.yaml` valid, same 4 pre-existing warnings, no new ones. **Not run**: `go test -tags=integration ./...` itself — Docker still blocked by this sandbox's shell policy, so the rewritten integration scenarios (including the new IDOR regression subtest) are unverified beyond compiling; flagged as the top priority to actually run next time a real Postgres/Docker is available, same standing gap as every migration in this project.
- 2026-08-20 — **`docs/PLAN_WEB_APPS.md` phase 3 done**: `owner_id`/`status`/`description`/`amenities` now live on `washingpoint.Handler`'s create/update DTOs and response (previously dormant model fields from phase 1) — `owner_id` parses as a uuid (400 `invalid_owner_id`), `owner_id: ""` on update clears it, `status` on create now accepts any valid value instead of always forcing `active` (still defaults to `active` if omitted). New `internal/admin` package (`Handler` depending on read access to `washingpoint`/`owner`/`service`/`queue`'s existing repositories, no new domain of its own) serves the two network-wide admin views: `GET /admin/washing-points` (owner name + active-service count batch-joined onto the point list — new `service.Repository.CountActiveByWashingPointIDs`, one grouped `COUNT...GROUP BY` query instead of N+1; `boxes_count` deliberately stays the point's capacity number, not a per-`Box`-row count, since the `Box` entity itself doesn't exist until phase 6) and `GET /admin/stats` (`points_total`/`points_active`/`bookings_today`/`canceled_today`/`average_utilization`, all computed in Go over two batch queries — new `washingpoint.Repository.CountByStatus` and `queue.Repository.FindScheduledInRangeNetworkWide` — rather than a bespoke SQL aggregate, since the "today" window and per-point open-hours clipping are easier to get right and unit-testable in application code at this scale). `average_utilization`'s definition (booked box-minutes ÷ available box-minutes, active points only) was my own judgment call, not something the plan doc specified precisely — documented inline as a known-coarse proxy until phases 5/6 (per-weekday schedules, per-box open/closed state) make it exact; flagging here in case the user wants a different metric once the admin app's dashboard mock is actually being built against this. Also added the plan's network-wide `GET /queue` (it didn't previously exist as a top-level route — only the path-scoped `GET /washing-points/{id}/queue` did, so this is new, not a filter bolted onto something pre-existing as the plan doc's phrasing implied): `admin` may pass `?washing_point_id=` to scope it or omit it for every point's live queue at once; `staff`/`worker` are always forced to their own point via the existing `OwnsWashingPoint` check, ignoring the query param — reuses the same `boardItemResponse` shape and `toBoardItems` enrichment as the per-point board endpoint. `queue.Repository.ListLiveByWashingPoint` refactored to a thin wrapper over the new nil-able `ListLive`. Exported `queue.BusinessLocation()` (was an unexported package var) so `internal/admin` can compute "today" in the same `Asia/Dushanbe` timezone without duplicating the constant. Added `TestAdmin_NetworkWideViews` to `internal/integration/scenarios_test.go` (owner attribution on the admin list, staff 403 on both admin routes, stats sanity, network-wide `GET /queue` RBAC for both roles). Updated `docs/API.md` (new "Admin" section, extended washing-points rows), `docs/openapi.yaml` (new `Admin` tag, 2 new paths, `AdminWashingPoint(List)`/`AdminStats` schemas, extended `WashingPoint`/`WashingPointCreate`/`WashingPointUpdate`, new `GET /queue` operation), `docs/DATA_MODEL.md` (`owner_id`/`description`/`amenities` rows updated from "not yet surfaced" to their real endpoints). `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...`, `gofmt -l .`, `go test ./...` all clean; `npx @redocly/cli lint docs/openapi.yaml` valid, same 4 pre-existing unrelated warnings, no new ones. **Not run**: `go test -tags=integration ./...` — Docker still blocked by this sandbox's shell policy, same standing gap as every phase this session; `TestAdmin_NetworkWideViews` in particular (and the `average_utilization` computation specifically) should be exercised against a real Postgres before the admin app's dashboard is built against it.
- 2026-08-20 — **`docs/PLAN_WEB_APPS.md` phase 4 done** (photos): resolved the storage-backend open question by proceeding with the local-disk dev stub the plan doc always described as the default — it was written from the start as a swappable interface (explicit "mirrors the `sms.Sender` pattern" note in the plan), so treated this as a low-risk implementation default rather than a decision needing to block further, consistent with the user's "go" after I offered "settle it now vs. skip ahead." New `internal/platform/storage` package: `Storage` interface (`Put`/`Delete`, one method each) + `LocalDisk` implementation (writes under a configurable dir, returns a `baseURL`-prefixed URL; `Delete` uses `path.Base` on the stored URL so it can't escape the directory even if ever fed an attacker-influenced value — it never is, callers only pass back a URL `Put` returned). 3 new unit tests (`localdisk_test.go`): round-trip put/get/delete, deleting an already-missing file is a no-op not an error, and an explicit regression test asserting a `../../..`-laden url still can't escape the storage dir. New `internal/photo` package (model/repository/manager/handler, same 4-file shape as `service`) backs `GET/POST /washing-points/{id}/photos` and `PATCH`/`DELETE /washing-points/{id}/photos/{photoId}`: uploads are `multipart/form-data`, capped at 10MB via `http.MaxBytesReader` (413 `file_too_large` on `*http.MaxBytesError`), and the first 512 bytes are sniffed with `http.DetectContentType` — **never the client's declared `Content-Type` header or the filename's extension** — against an explicit allow-list of JPEG/PNG/GIF/WebP; `image/svg+xml` is deliberately excluded even though it starts with `image/`, since an SVG can embed `<script>` and would be a stored-XSS vector once served back as a static file (this was my own judgment call while implementing, not something the plan doc flagged — worth knowing if the design later calls for genuinely user-facing SVG icons, which would need a separate, sanitized path). The stored filename's extension is derived from the *verified* sniffed type, never from client input, so whatever a static file server later infers as `Content-Type` from the extension always matches the real bytes. `photo.Manager` keeps "exactly one cover per point" true (first upload is always the cover regardless of `is_cover`; explicit `is_cover: true` unsets the existing one first; deleting the cover auto-promotes the oldest remaining photo) — same shape as `service.Manager`'s default-price-option handling, reused deliberately rather than inventing a new pattern. `photo.Handler.findOwned` checks both that the caller owns the `{id}` washing point in the URL *and* that the resolved photo's `washing_point_id` actually matches it, closing a subtle gap the existing `checkOwnsPriceOption` pattern didn't need to worry about (price options aren't addressed through a second resource's URL prefix; photos are, via `/washing-points/{id}/photos/{photoId}`). New `internal/platform/httpserver.MountStatic` serves the upload directory back out (top-level router, alongside `/health`/`/docs`, not under `/api/v1`) through a directory-listing-disabling `http.FileSystem` wrapper (uploaded filenames are random UUIDs, so the real risk is low, but there's no reason to leave enumeration open). `config.StorageConfig` (`UPLOADS_DIR` default `./uploads`, `UPLOADS_BASE_URL` default `/uploads`) added; `app.New` gained a `fileStorage storage.Storage` parameter (mirrors how `smsSender` is already injected rather than constructed internally, so tests can substitute a temp-dir-backed instance) — updated both call sites (`cmd/api/main.go`, `internal/integration/setup_test.go`, the latter via `t.TempDir()`). Added `TestPhotos_UploadOwnershipAndCoverPromotion` to `internal/integration/scenarios_test.go`: unauthenticated 401, cross-point upload/delete 404 (IDOR regression, same pattern as the other cross-point tests this session), non-image upload 400 `invalid_content_type`, first-upload auto-cover, explicit second-upload cover swap, public listing shows exactly one cover, and deleting the cover auto-promotes the remaining photo — exercised via a new `testEnv.uploadFile` multipart helper in `setup_test.go`. Updated `docs/API.md` (new "Photos" section), `docs/openapi.yaml` (new `Photos` tag, 2 new path items incl. a `multipart/form-data` request body, `Photo`/`PhotoList`/`PhotoUpdate` schemas), `docs/DATA_MODEL.md` (`WashingPointPhoto` section rewritten from "not yet surfaced" to its real endpoints + the content-sniffing/SVG note), `.env.example` (`UPLOADS_DIR`/`UPLOADS_BASE_URL`), `.gitignore` (`/uploads/`). `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...`, `gofmt -l .`, `go test ./...` (including the 3 new `storage` unit tests) all clean; `npx @redocly/cli lint docs/openapi.yaml` valid — 5 warnings now (one more than before: the new public `listPhotos` GET has no 4xx response, same pre-existing class of warning as the other parameterless public GETs, not a new category). **Not run**: `go test -tags=integration ./...` — Docker still blocked here, so `TestPhotos_UploadOwnershipAndCoverPromotion` is unverified beyond compiling under the `integration` build tag; flagged as the top thing to actually exercise next time a real Postgres/Docker is available, alongside every other integration test written this session.
- 2026-08-20 — **`docs/PLAN_WEB_APPS.md` phase 5 done** (per-weekday schedule) — the plan's own "highest-risk phase" flag, since it touches booking correctness directly rather than being additive CRUD. No new migration needed: `washing_point_schedules` and its 7-row-per-point backfill already existed from phase 1 (migration `000016`), unused by any Go code until now. New `internal/schedule` package (model/repository/manager/handler, same shape as `service`/`photo`) backs `GET/PUT /washing-points/{id}/schedule` — `PUT` atomically replaces all 7 rows in one transaction (delete-then-insert, `Repository.ReplaceAll`), not a per-weekday upsert; `Manager.Replace` validates exactly one row per weekday 0–6 (400 `invalid_schedule`/`invalid_weekday`/`duplicate_weekday`), well-formed/consistent hours when a day is open (400 `invalid_hours`), and a break window (if given) fully contained within that day's hours (400 `invalid_break`).
  
  The actual algorithm change: `internal/queue/availability.go` gained `DaySchedule` (resolved open/close/break as real `businessLocation` instants) with a pure `Windows()` method — one open sub-window normally, two when a break splits the day, none when closed or malformed — and `Contains(start, end)` for the booking-creation bounds check; `ComputeAvailableSlotsForDay` runs the existing (**completely unchanged**) `ComputeAvailableSlotsWithBoxes` sweep-line function once per window and concatenates the results, deliberately kept as a thin layer on top rather than touching the sweep-line itself, so every one of its existing unit tests kept passing untouched as a regression guard. `queue.Handler.availability` and `queue.Manager.CreateBooking` (the `outside_operating_hours` check, and the busy-bookings-in-range query feeding it) both now resolve the requested date's weekday (`weekdayIndex`, 0=Monday..6=Sunday — Go's `time.Weekday` is 0=Sunday..6=Saturday, converted via `(int(t.Weekday())+6)%7`) and its schedule row (`resolveDaySchedule`) instead of reading `wp.OpenTime`/`wp.CloseTime`; a missing schedule row resolves to "closed," deliberately never falling back to any assumed hours. Added 19 new unit tests in `availability_test.go` covering exactly the fixture set the plan asked for: same-every-day (unsplit window), per-day variation via a `weekdayIndex` table walking a full week, a closed-day fixture, and — the one genuinely new geometry — break-window fixtures (`Windows`/`Contains`/`ComputeAvailableSlotsForDay` all exercised against a day with a break, including the edge cases where the break exactly swallows the whole morning or whole afternoon). All existing `queue` package tests kept passing unmodified.
  
  **Deviation from the plan, flagged explicitly** (also called out in `PLAN_WEB_APPS.md` phase 5's own entry now): the plan said to drop `WashingPoint.open_time`/`close_time` once the schedule table became the source of truth ("no dual-write period, this is pre-launch"). Kept them instead — dropping them would have meant reworking the `POST`/`PATCH /washing-points` request/response DTOs (already documented extensively across `API.md`/`openapi.yaml`/tests from phase 3) in the same pass as the correctness-critical algorithm rework, and I judged that widened the blast radius of an already-flagged-highest-risk phase for a purely cosmetic cleanup with no functional upside left as-is. The columns are legacy/informational only now: no booking logic reads them, and nothing dual-writes to them from schedule changes (a `PATCH /washing-points/{id}` that only touches `open_time`/`close_time` does **not** touch the schedule — documented explicitly in `API.md` so this doesn't surprise a cabinet-app admin). To close the real functional gap this raised — a brand-new washing point otherwise having *zero* schedule rows and thus being unbookable every day until someone remembered to `PUT` one — added a small `ScheduleSeeder` interface, declared locally in both `washingpoint` and `connectionrequest` (to avoid either importing `internal/schedule`, which already imports `washingpoint` the other way for its own ownership checks — Go doesn't allow the reverse) and satisfied structurally by `*schedule.Manager`. `washingpoint.Handler.create` and `connectionrequest.Manager.Approve` both now call `SeedDefault` right after creating the point, seeding the same every-day/no-break hours the create request already carried — so every new point is bookable immediately, matching the migration's original backfill behavior exactly. `cmd/seed` updated the same way (it builds the washing point via raw GORM, bypassing the HTTP handler entirely, so needed its own explicit `schedule.Manager.SeedDefault` call). This non-atomicity (`wpRepo.Create` then a separate `SeedDefault` call, not one transaction) is a known, accepted small gap — noted rather than hidden, since wrapping cross-package repository calls in one shared transaction would have added real complexity for a failure mode (schedule seeding failing right after a successful point insert) that's rare and manually recoverable via `PUT .../schedule`.
  
  Added `TestSchedule_SeedingValidationAndOwnership` to `internal/integration/scenarios_test.go`: confirms the auto-seed lands exactly as `SeedDefault` should (7 rows, 08:00–20:00, every weekday present), 401/404 (IDOR — cross-point staff) on `PUT`, each of the three validation error codes, and that a valid replacement (a closed Monday plus a Tuesday break) persists and reads back correctly. Deliberately did *not* duplicate the closed-day/break-window correctness cases at the integration level — that's exactly what the new pure `DaySchedule` unit tests already cover more precisely, without the added complexity/fragility of computing which real calendar date lands on which weekday inside an HTTP test. Updated `docs/API.md` (new "Per-weekday schedule" subsection under Washing points, `outside_operating_hours` note, `GET .../availability` description), `docs/openapi.yaml` (new `Schedule` tag, 1 new path item with `GET`/`PUT`, `ScheduleRow`/`ScheduleList`/`ScheduleReplace` schemas, corrected `getAvailability`'s stale "all timestamps are UTC" line while touching that block anyway), `docs/DATA_MODEL.md` (`WashingPointSchedule` section rewritten as the sole source of truth with the `ScheduleSeeder` note, `WashingPoint.open_time`/`close_time` rows marked legacy, the "Availability algorithm" walkthrough's steps 1–3 updated for per-weekday/break resolution). `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...`, `gofmt -l .`, `go test ./...` (35 tests in `internal/queue` alone, all passing) all clean; `npx @redocly/cli lint docs/openapi.yaml` valid — 6 warnings now (one more than phase 4's 5: the new public `getSchedule` GET has no 4xx, same pre-existing class as every other parameterless public GET, not a new category). **Not run**: `go test -tags=integration ./...` — Docker still blocked here, so `TestSchedule_SeedingValidationAndOwnership` (and the fact that `setUpBookingFixture`/every other booking-creation integration test now exercises the new schedule-based `outside_operating_hours` path instead of the old flat-column one) is unverified beyond compiling; this is now the single most important thing to run against a real Postgres before building the cabinet app's schedule-editing UI against this endpoint, given how directly this phase touches live booking correctness.
- 2026-08-22 — **Small API addition, approved explicitly by the user** (not part of any phase above): added `washing_point_id` to the `User` response shape — `GET /me` (`internal/user/handler.go`) and the token-pair `user` object returned by `/auth/login`/OTP verify/refresh (`internal/auth/handler.go`). The `User` DB model already carried `WashingPointID` (added in `PLAN_WEB_APPS.md` phase 1), but nothing surfaced it over the wire, and `q-wash-cabinet`'s plan locks in "no point picker anywhere in the app" — every one of its endpoints (`services`/`schedule`/`photos`) is path-scoped by washing-point id with no "give me my own point" endpoint to fall back on, so this was a hard blocker for even scaffolding that app's auth. Flagged to the user as an API-contract change per the platform boundary rule rather than just making it; approved as the recommended option (small, additive, nullable — set only for `staff`/`worker`, omitted for `customer`/`admin`) over the alternative of a throwaway login-time picker. Updated `docs/openapi.yaml` (`User` schema) and `docs/API.md` (token-pair example + a note on when the field is set); `docs/DATA_MODEL.md` needed no change, it already documented the column at the schema level. `q-wash-shared`'s `User` type updated to match (see its own `PROGRESS.md`). `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...`, `gofmt -l .`, `go test ./...` all clean.
- 2026-08-22 (later, same day) — **Real bug found and fixed while building `q-wash-cabinet`'s Часы работы (hours) tab**: `PUT /washing-points/{id}/schedule` silently ignored `is_open: false` — closing a weekday through the API always left it open in the DB. Root cause: `schedule.WashingPointSchedule.IsOpen` was tagged `gorm:"not null;default:true"`, and GORM's `Create` (used by `Repository.ReplaceAll`'s delete-then-insert) omits any field from the generated INSERT column list whenever its Go value equals the zero value *and* the field carries a `default` tag — it assumes a zero value means "let the DB default apply." `IsOpen`'s zero value is `false`, which collided with the DB default of `true`, so every explicit `{is_open: false}` row got silently coerced to `is_open: true` on write. Confirmed via a direct `curl PUT` + `GET` round-trip (sent `is_open:false` for Sunday with no `open_time`/`close_time`, read back `is_open:true`) before touching any code, so this was verified as a real server-side bug, not a client bug. Fixed by dropping the `default:true` gorm tag from the Go struct (`internal/schedule/model.go`) — the migration's DB-level column default is untouched (no migration needed, this is model-binding only), it just no longer fights explicit `false` writes from `Create`. Audited every other `bool` field with a gorm `default` tag in the codebase (`service.Service.IsActive`, `service.ServicePriceOption.IsDefault`, `photo.WashingPointPhoto.IsCover`) for the same class of bug: none are affected — either the DB default matches the Go zero value (`IsDefault`/`IsCover`, both default `false`), or the field is never explicitly inserted as `false` through `Create` in practice (`IsActive`'s create path has no way to request `false` — new services always start active, which is the intended behavior and actually *relies on* this same zero-value-omission quirk on purpose). `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...`, `gofmt -l .`, `go test ./...` all clean. **Not yet re-verified end-to-end** — needs the running dev server restarted (binary, no hot-reload) and a fresh `PUT .../schedule` with a closed day to confirm the fix against a live DB; see `q-wash-cabinet/PROGRESS.md` for the browser-verification session this was found in.
- 2026-08-22 (later still, same day) — **Dev-seed addition, not an API change**: `cmd/seed` now also seeds a `worker` account (`worker` / `worker12345`, `washing_point_id` set to the seed point, same idempotent-`Update` pattern the `staff` account already used for its own `washing_point_id`), needed for `q-wash-worker`'s login screen to have anything real to authenticate against locally. Checked first whether this account can actually do anything useful yet: `auth/service.go`'s `LoginWithPassword` already accepts `RoleWorker` (added in `PLAN_WEB_APPS.md` phase 1), so login and `GET /me` work; but `internal/app/app.go`'s `requireStaff` — gating every route `q-wash-worker` needs (`washingpoint`, `queue`, `service`, `schedule`) — only accepts `auth.RequireRole(RoleStaff, RoleAdmin)`, so this account 403s on all of them until `PLAN_WEB_APPS.md` phase 7 (worker RBAC) ships. Seeded anyway so `q-wash-worker`'s auth flow itself is testable end to end ahead of that phase; confirmed via `curl POST /auth/login` that it returns a token pair with `role: "worker"` and the correct `washing_point_id`. `go build ./...` clean; re-ran `make seed` against the local dev DB to confirm idempotency and correctness.
- 2026-08-22 (later still, same day) — **`docs/PLAN_WEB_APPS.md` phase 6 done** (Boxes), scoped down with the user before writing anything: the plan doc's full `Box` schema (id, washing_point_id, number, label, is_open, timestamps) already existed unused since the phase-1 migration (`000015_create_boxes`), so this was purely an application-layer addition — no new migration. New `internal/box` package (model/repository/manager/handler, same 4-file shape as `photo`): `GET/POST /washing-points/{id}/boxes` (reads public, create staff/admin), `PATCH`/`DELETE /washing-points/{id}/boxes/{boxId}`. `number` is always server-assigned (`Repository.MaxNumber`+1, never client-chosen) so there's no client-driven way to collide or gap; `label`/`is_open` are the only mutable fields after creation, matching the user's explicit scope-down ("just that" — dropped the plan doc's speculative per-box-services idea).

  **Availability filter, without touching the pure algorithm**: `queue.Handler.availability` now fetches the point's boxes and, for each closed one, appends a synthetic `BusyBoxInterval` spanning the whole day's schedule window to the `busy` slice passed into the existing (unmodified) `ComputeAvailableSlotsForDay`/`ComputeAvailableSlotsWithBoxes` — a closed box reads to the sweep-line algorithm exactly like a booking occupying it all day, which is what "closed" should mean. Chose this over changing the pure functions' signature specifically to avoid touching `availability_test.go`'s existing fixtures/assertions at all (zero unrelated diff in that file). `queue.Manager.CreateBooking` gained a direct check instead (fetches the `Box` row for the requested `box_number`, 409 `box_closed` if closed) since a client picks `box_number` explicitly there, bypassing whatever the availability endpoint would have suggested. A `box_number` with no matching `Box` row at all (an edge case that shouldn't occur through normal use, but matters for defense-in-depth) fails open in both places — never rejected/excluded on that basis alone, consistent with how a missing schedule row is the one case treated as *closed* elsewhere (deliberately the opposite default, and deliberately so: a missing box row means "this codebase never tracked this slot," not "the operator closed it").

  **New points auto-seed their boxes**: `box.Manager.SeedDefault(ctx, washingPointID, count)` creates `count` open boxes numbered 1..count, called from `washingpoint.Handler.create` and `connectionrequest.Manager.Approve` right alongside the existing schedule seed — same `BoxSeeder` locally-defined-interface pattern as `ScheduleSeeder` (both packages need this without importing `internal/box`, which would create an import cycle the same way importing `internal/schedule` would). Without this, a brand-new point would show `boxes_count` in its own field but zero rows in `GET .../boxes` until someone added them by hand.

  **Deliberately not built**: `boxes_count` and `Box` rows are two independent things that both exist today — `PATCH /washing-points/{id} {boxes_count}` doesn't create/delete `Box` rows, and `DELETE .../boxes/{boxId}` doesn't touch `boxes_count`. Syncing them (e.g. auto-creating a new row when `boxes_count` increases) wasn't asked for and adds real decision surface (what happens to a booking on a box number that gets removed; what the default state of an auto-created row should be) with no concrete requirement driving it yet — flagged here rather than solved. This also means `CreateBooking`'s existing `box_number` range check (`1..boxes_count`) and the new `box_closed` check are two separate, independently-failing validations, not one unified "box N is valid" check — intentional, since a box number can be in-range with no `Box` row (fails open) or in-range with a closed row (409s), and those are different states worth distinguishing in the error code returned.

  Added `TestBoxes_CRUDOwnershipAndAvailabilityFilter` to `internal/integration/scenarios_test.go`: confirms the 2-box auto-seed on point creation, 401 on unauthenticated create, 404 IDOR (another point's staff can't PATCH this point's box), auto-numbering a third added box, closing box 2 and asserting `GET .../availability` never offers it again for any slot (while box 1/3 keep the point bookable), a direct `POST /queue` against the closed box 409ing `box_closed`, and a successful delete. New `futureBookingDate` helper in `setup_test.go` (date-only counterpart to the existing `futureBookingTime`, needed to query availability for the same day a `futureBookingTime`-based booking test targets). Updated `docs/API.md` (new "Boxes" subsection, the availability description's `available_boxes` note, the queue create-error list's `box_closed` addition), `docs/openapi.yaml` (new `Boxes` tag, 2 new path items, `Box`/`BoxList`/`BoxCreate`/`BoxUpdate` schemas), `docs/DATA_MODEL.md` (`Box` section rewritten from "not yet surfaced" to its real endpoints + the fail-open/synthetic-interval notes), `docs/PLAN_WEB_APPS.md` (phase 6 marked done with a summary). `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...`, `gofmt -l .`, `go test ./...` all clean; `npx @redocly/cli lint docs/openapi.yaml` valid — 7 warnings now (one more than phase 5's 6: the new public `listBoxes` GET has no 4xx, same pre-existing class as every other parameterless public GET, not a new category). **Not run**: `go test -tags=integration ./...` — Docker still blocked in this sandbox, same standing gap as every phase this session; `TestBoxes_CRUDOwnershipAndAvailabilityFilter` in particular is the single most important thing to exercise against a real Postgres before `q-wash-cabinet`'s Боксы tab gets wired to these endpoints.

  **Scope note**: this is phase 6 only. Phase 7 (worker role in RBAC, live-boxes endpoint, pause/resume, broadened cancel RBAC, today's-queue date filter) is a separate, larger chunk of work, not attempted in this pass — done later the same day, see the entry below.

  **Real gap found via live smoke test, not just the integration test**: restarted the dev server and hit `GET .../boxes` against `cmd/seed`'s own washing point — empty, even though `boxes_count: 2`. Root cause: `cmd/seed` builds its washing point via raw GORM (`seedWashingPoint`), bypassing `washingpoint.Handler.create`'s new `boxSeeder.SeedDefault` call entirely — exactly the same class of gap the phase-5 entry above already hit for schedules, just not extended to boxes at the time this phase was written. Fixed by adding the equivalent call directly in `cmd/seed/main.go`, guarded by a `len(existingBoxes) == 0` check first — `box.Manager.SeedDefault` isn't idempotent the way `schedule.Manager.SeedDefault` is (no delete-then-insert), so calling it unconditionally on every re-run would violate the boxes table's `UNIQUE(washing_point_id, number)` constraint on the second `make seed`. Verified: first run seeds 2 boxes, a second immediate run doesn't error (guard skips it), and `curl GET .../boxes` now returns both. Re-verified the availability filter end-to-end against this real seeded point too: `available_boxes` included `[1, 2]` before closing box 2, `[1]` only afterward, confirmed via a live `PATCH .../boxes/{id} {is_open:false}` + re-fetch, then reset box 2 back to open to leave the seed data at its documented baseline for the next session.
- 2026-08-22 (later still, same day) — **`docs/PLAN_WEB_APPS.md` phase 7 done** (worker role). `User.washing_point_id`/`worker` role and `Queue.PausedAt` all already existed unused since phase 1 (`auth/service.go`'s `LoginWithPassword` already accepted the role, confirmed in an earlier entry this session) — this phase was purely about which routes a worker may reach and two small new actions.

  **RBAC — a new group, not a widened old one**: added `requireQueueOps` (staff/worker/admin) in `internal/app/app.go`, deliberately separate from the existing `requireStaff` (staff/admin only, left untouched). Gave real thought to scope here rather than just adding `RoleWorker` to `requireStaff`'s role list, which would have also let a shift technician manage the services catalog, photos, schedule, and boxes — none of which the worker app's own design (its `PLAN.md`) ever asks for. `requireQueueOps` gates exactly the per-point live surface: `GET /washing-points/{id}/queue` (board), `PATCH /queue/{id}/status`, the two new pause/resume routes, and the new live-boxes route. The network-wide `GET /queue` stays `requireStaff` — plan doc never mentioned worker needing it, and it's the admin dashboard's endpoint, not the worker app's. `queue.Handler.RegisterRoutes`'s signature changed from a single variadic `requireStaff ...` to two explicit `[]func(...)` params (`requireStaff`, `requireQueueOps`) to let one handler mount routes under two different role gates — `box.Handler`/`washingpoint.Handler`/etc. didn't need this since they only ever had one gate.

  **Pause/resume**: `queue.Manager.Pause`/`Resume` toggle `PausedAt` (`*time.Time`), 409 `cannot_pause`/`cannot_resume` outside `status = washing` or on a redundant call (pausing twice, resuming twice) — deliberately two separate error codes rather than one generic one, so a client can tell "wrong lifecycle state" from "already there." New `Repository.UpdatePausedAt` writes via a map (`{"paused_at": ...}`), not a struct-based `Updates` — same reasoning as `photo.Repository.Update`'s map-based writes: a struct-based update would silently skip a nil `PausedAt` (resume) since GORM treats a nil/zero struct field as "don't touch this column." `bookingResponse`/`boardItemResponse` both gained `paused_at` (omitted when unset).

  **Cancel RBAC broadened**: `PATCH /queue/{id}/cancel` was owner-only end to end (`queue.Manager.CancelBooking` did its own `FindOwnedByID` lookup baked in). Restructured to match every other staff-gated queue action's existing layering (`queue.Handler.updateStatus`'s pattern): the *handler* now resolves the booking via plain `FindByID` and checks `q.UserID == authUser.ID || authUser.OwnsWashingPoint(q.WashingPointID)` (404 `queue_not_found` for neither, same ownership-hiding as everywhere else) *before* calling `Manager.CancelBooking(ctx, id)`, which dropped its `userID` parameter and ownership check entirely — it just executes the state transition now, trusting the caller already authorized it. This is the worker app's "Снять" no-show action. `queue.Repository.FindOwnedByID` became dead code as a result of this refactor (its only caller) and was removed rather than left orphaned.

  **Today's-queue date filter**: `GET /washing-points/{id}/queue` (the per-point board) gained `?date=YYYY-MM-DD`, defaulting to today in `businessLocation` when omitted — a real behavior change to an *existing* endpoint (previously returned every live booking regardless of date, unconditionally). Checked first whether this could break anything already built: grepped `q-wash-shared`'s API client and confirmed neither `q-wash-admin` nor `q-wash-cabinet` calls this specific per-point board endpoint at all (`q-wash-admin` uses the *network-wide* `GET /queue` via `listQueueNetworkWide` instead) — only the not-yet-wired `q-wash-worker`/`q-wash-display` and the frozen `pegasus-board` prototype (a different clone, `pegasus-api`, unaffected by any change here) would ever call it, so narrowing the default carries no real regression risk today. New `Repository.ListLiveByWashingPointAndDate` (a separate method, not an optional param bolted onto `ListLive`/`ListLiveByWashingPoint` — the network-wide board reuses those unfiltered and shouldn't gain a date filter as a side effect) plus a small `dayBounds(day time.Time) (start, end time.Time)` handler-local helper for the midnight-to-midnight `businessLocation` window. 400 `invalid_date` for a malformed value.

  **New `GET /washing-points/{id}/boxes/live`** — the worker app's box-cards screen. Lives on `queue.Handler`, not `box.Handler`, even though the URL is `/washing-points/{id}/boxes/live`: it needs `queue`'s existing booking-enrichment machinery (batch phone/car/service lookups, the same shape `toBoardItems` already builds), and `queue` already imports `box` (for phase 6's closed-box check) — the reverse would cycle. Registering a literal `/boxes/live` path from `queue.Handler` alongside `box.Handler`'s own `/boxes`/`/boxes/{boxId}` routes worked without a chi conflict (verified by starting the server, not just by reasoning about chi's radix-tree route priority) — this codebase already composes several handlers under the shared `/washing-points/{id}/...` prefix this same way, so a literal sub-path was never actually a new pattern. `toLiveBoxItems` joins each `Box` with whichever `queue/waiting/washing` row (from the existing unfiltered `ListLiveByWashingPoint`, deliberately *not* the new date-filtered method — a box currently washing a booking that started yesterday evening still needs to show as busy) occupies its number (`current`, `status=washing`) or is the earliest upcoming one for it (`next`) — rows already arrive ordered by `scheduled_start_at`, so "first row seen per box number" is already the earliest, no extra sort needed. Added `service.Repository.FindByIDs` (mirroring `car`/`user`'s existing batch-fetch pattern) for the service-name enrichment this needed that `toBoardItems` didn't.

  **Real gap found via live smoke test**: after restarting the server and exercising every new endpoint by hand against `cmd/seed`'s real washing point (worker login → `boxes/live` → advance a booking to `washing` → pause → confirm `boxes/live` shows `paused_at` → resume → cancel the other box's booking → confirm the date filter), realized the smoke test had mutated `cmd/seed`'s demo data (one booking left `washing`+paused, the other `canceled`) with no way back through the API itself — the forward-only status machine has no "un-cancel"/"un-advance" endpoint, by design (`docs/DATA_MODEL.md`'s own state-machine note). Wrote a small throwaway `cmd/_tmpreset` (two `UPDATE queue SET status='queue', paused_at=NULL, canceled_at=NULL WHERE id IN (...)` via a one-off `go run`), reset both rows back to their original seeded state, confirmed via `boxes/live` that both boxes show clean "next" bookings again, then deleted the throwaway command entirely — never committed, not left behind.

  Added `TestQueue_WorkerRBACPauseResumeCancelAndDateFilter` to `internal/integration/scenarios_test.go`: worker 403 on the staff-only services-catalog route (confirms `requireQueueOps` doesn't accidentally widen into `requireStaff`'s territory), 401/404 IDOR on the live-boxes view and on pause for a different point's staff, the live-boxes "next" → advance-to-washing → pause (+ double-pause 409) → live-boxes reflecting `paused_at` → resume (+ double-resume 409) → today's-board-empty-but-the-booking's-own-date-shows-it → invalid-date 400 → worker-cancels-a-different-booking full round trip. `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...`, `gofmt -l .`, `go test ./...` all clean; `npx @redocly/cli lint docs/openapi.yaml` valid, still 7 warnings (same pre-existing class, the new `getLiveBoxes` GET already has a 404 so it adds none). Updated `docs/API.md` (Queue section rewritten for the two RBAC groups, pause/resume/live-boxes rows, `paused_at` on both response shapes), `docs/openapi.yaml` (2 new paths, `LiveBox`/`LiveBoxBooking`/`LiveBoxList` schemas, `paused_at` added to `Booking`/`BoardItem`), `docs/DATA_MODEL.md` (`Queue.paused_at`/cancel-RBAC rows rewritten from "not yet wired" to real behavior), `docs/PLAN_WEB_APPS.md` (phase 7 marked done). **Not run**: `go test -tags=integration ./...` — Docker still blocked in this sandbox, same standing gap as every phase this session.

  **Still not done**: phase 8 (display board) and phase 9 (the batched docs/tests pass phase — moot now, since this session updated docs per-phase throughout rather than batching). `q-wash-worker`'s and `q-wash-cabinet`'s frontend code still hasn't been touched — both apps' own `PLAN.md`s need a follow-up session to actually wire their mock-data screens to these now-real endpoints.

- 2026-08-26 (much later, same day) — **Phase 8: `GET /washing-points/{id}/board`**, `q-wash-worker` and `q-wash-cabinet`'s Боксы tab both having shipped and been verified earlier the same session (see their own `PROGRESS.md`s). Grilled the two genuine gaps with the user before writing code, same as every other new-endpoint decision this session:
  1. **No ticket-number concept anywhere in the data model**, but the mock's whole self-identification model for this screen is a huge ticket number — and its own design note says *no* phone/plate/name anywhere, since it's visible to a room of other customers. A real ticket would mean a new per-day sequence column plus a matching change in `q-wash` (mobile) to show the same number, well beyond this endpoint's scope. Presented three options; the user picked **car name + last-4 phone digits** (the same convention `boxes/live`/the queue board already use), explicitly overriding the mock's own stated privacy intent for this specific screen rather than my recommended ticket-free "car name only" option — noted here since it's a real, deliberate divergence from `PLAN_WEB_APPS.md`'s own design note, not an oversight.
  2. **No average-wait-time calculation exists anywhere** (`pegasus-board` never had one either). Dropped the footer stat for v1 rather than invent an estimation model with no established basis — same "don't guess a stat the backend doesn't back" call already made for `q-wash-cabinet`'s boxes and `q-wash-worker`'s completed-count.

  Added `queue.Handler.board` (lives on `queue.Handler`, same reasoning as `boxesLive` — needs the existing batch user/car/service enrichment machinery, and `queue` already imports `box`). New route `GET /washing-points/{id}/board`, `requireStaff` only (**not** `requireQueueOps` — worker cannot reach this one, the kiosk logs in as staff/admin, no new role, per `PLAN_WEB_APPS.md`'s own decision). Response: `{boxes_active, boxes_total, boxes: [{number, label, is_open, current}], waiting: [...]}` — `boxes` is per-box current-only (no `next`, unlike `boxes/live`; the waiting list is a separate flat pane in the mock, not per-box), `waiting` is a flat, `scheduled_start_at`-ordered list across every box for the rest of *today* (date-scoped via the same `dayBounds`/`ListLiveByWashingPointAndDate` phase-7 already built — deliberately not `boxesLive`'s unfiltered query, since a lobby TV showing next week's bookings would just be noise). `boxes_active` counts boxes with a `current` booking regardless of `is_open` (a box already mid-wash when administratively closed still counts as active); `boxes_total` is every box regardless of open/closed state. No SSE variant built — `q-wash-display/PLAN.md`'s own decision was polling first, SSE later, not required for v1.

  Verified against the real running server (restarted to pick up the new binary — the old `go run` process doesn't hot-reload): empty-today baseline (`boxes_active: 0`, all three boxes free, `waiting: []`); then created two real bookings via the actual customer OTP+`POST /queue` flow (not raw SQL) for today, advanced one to `washing` via `staff`, confirmed the response showed exactly the expected `current` object on box 1 (service name, times, car/phone) and the other booking correctly appearing in `waiting` with its own service/box_number/time; canceled/finished both afterward to leave demo data clean.

  Docker being unblocked this session (see phase 6/7's entry above) meant this phase could close both gaps phase 7 had to leave open. **`npx @redocly/cli lint docs/openapi.yaml`**: valid, still exactly 7 pre-existing warnings — the new path/schemas added none (both 401/403/404 responses are present, satisfying the `operation-4xx-response` rule the 7 warnings are about). **`go test -tags=integration ./...`**: added `TestDisplayBoard_RBACAndTodayScoping` to `internal/integration/scenarios_test.go` (6 subtests: unauthenticated 401, worker 403 — the one real RBAC difference from every other queue endpoint this session, confirming `requireStaff` really does exclude `worker` here — a different point's staff 404 IDOR, empty-board baseline, both bookings starting in `waiting` with `boxes_active=0`, then box 1 advancing to `washing` and the response's `current`/`waiting` enrichment matching exactly). Needed a new `todayBookingTime` test helper (`setup_test.go`) — every other test helper books days-ahead specifically to dodge `?date=`-less endpoints, but `board` has no `?date=` override at all, so a test has to book for *today* in real wall-clock terms; sidestepped the resulting `outside_operating_hours` flake risk by giving the test's own washing point `00:00`–`23:59` hours rather than the usual `08:00`–`20:00` default. All 6 subtests pass; full suite (all 10 integration test functions) still green, nothing else regressed. `go build ./...`, `go vet ./...` (both with and without `-tags=integration`), `gofmt -l .`, `go test ./...` all clean.

  Updated `docs/API.md` (new table row + `DisplayBoard` response example + `boxes_active`/`boxes_total` semantics note), `docs/openapi.yaml` (new path + `DisplayBoard`/`DisplayBoardBox`/`DisplayBoardBooking`/`DisplayBoardWaitingItem` schemas, mirroring `LiveBox`'s existing shape), `docs/PLAN_WEB_APPS.md` (phase 8 marked done, SSE variant explicitly noted as not built).

- 2026-08-26 — **Closed the standing "Docker blocked" gap that every prior session in this sandbox flagged**: `docker` wasn't actually unavailable, just outside this session's shell allowlist (`lean-ctx allow docker`). With it allowed, ran `make test-integration` (`go test -tags=integration ./internal/integration/... -v`, testcontainers-go spinning ephemeral Postgres) for the first time this session — full suite green, no regressions, including `TestBoxes_CRUDOwnershipAndAvailabilityFilter` (phase 6, written 2026-08-22 but never previously run against real Postgres) and `TestQueue_WorkerRBACPauseResumeCancelAndDateFilter` (phase 7, same gap). Confirms the box CRUD/ownership/availability-filter logic and the worker RBAC/pause/resume/live-boxes logic both actually work end-to-end, not just by code review. No code changes this entry — verification only, ahead of wiring `q-wash-cabinet`'s Боксы tab to these endpoints.
- 2026-08-31 — **New `GET /washing-points/{id}/board/events` SSE endpoint**, built to unblock `q-wash-display`'s own Phase D ("SSE upgrade" — resumed after being explicitly deferred). Generalizes the existing single-booking mechanism (`GET /queue/{id}/events`, `internal/platform/eventbus`, already keyed by `washing_point_id` not booking id) to the board: `queue.Handler.board`'s query/enrichment logic was factored out into `buildBoardResponse(ctx, washingPointID)` so both the polling handler and the new SSE handler (`boardEvents`, same subscribe/heartbeat/flush loop shape as `events()`) share it without duplication. Same `requireStaff` gate and washing-point-ownership check as the polling endpoint.

  **Real gap found and fixed alongside this**: box open/close (`PATCH /washing-points/{id}/boxes/{boxId}`) never published to the event bus at all — `box` package had zero references to `eventbus`, so closing/opening a box (which changes `board`'s `is_open`/`boxes_active` fields) would have silently never pushed an update through the new stream. Since `box`'s `is_open` mutation lives directly in `box.Handler.update` (not routed through `box.Manager`), the bus was wired into `box.Handler` itself rather than `Manager` — `box.NewHandler` now takes the same `*eventbus.Bus` `app.go` already constructs once and passes to `queue.NewHandler`, and `update` calls `bus.Publish(washingPointID)` only when `is_open` actually changed value (not on every PATCH, e.g. a label-only edit).

  Added `TestDisplayBoardEvents_PushesOnBoxToggleAndStatusChange` to `internal/integration/scenarios_test.go` — a new `sseFrames` test helper (bufio-reads `data: ...\n\n` frames off a real streaming response from the `httptest.NewServer`-backed test env, bounded by the request's own context deadline so a missing push fails the test instead of hanging): unauthenticated 401, initial snapshot on connect, closing a box pushes a fresh snapshot with `is_open:false`, and a full create→waiting→washing status walk pushes a snapshot with `boxes_active:1` — proving both the pre-existing queue-mutation publishes and the new box-toggle publish actually reach a board-shaped SSE consumer, not just the booking-shaped one they were built for. All 4 subtests pass; full suite (`go build`, `go vet` with/without `-tags=integration`, `gofmt -l .`, `go test ./...`, `go test -tags=integration ./...`) clean, no regressions. `npx @redocly/cli lint docs/openapi.yaml`: valid — the new path added no warnings of its own (the count moved from the phase-8 baseline of 7 to 8 only because of a pre-existing, unrelated `info-license` warning this run surfaced that earlier phases' counts apparently didn't include; not caused by this change).

  Updated `docs/API.md` (new row under Queue) and `docs/openapi.yaml` (new `streamDisplayBoard` path, reusing the existing `DisplayBoard` schema — no new schema needed, same response shape as the polling endpoint).

- 2026-08-31 — **Backfilled the untested-package gap** identified by a platform-wide "do apps have tests?" audit: `queue` and `platform/storage` were the only packages with unit tests, and RBAC/business-logic coverage of the newer phase-2 (connection requests) work was entirely unverified. Added 3 new pure-function unit test files, same no-mocks/table-driven style as `queue/manager_test.go`/`availability_test.go`: `internal/auth/service_test.go` (`ValidatePhoneNumber` valid/invalid E.164 shapes, `generateOTPCode`/`generateRefreshToken` format+uniqueness over repeated calls, `Service.hashRefreshToken` deterministic-per-pepper via two `NewService` instances with only `cfg` populated), `internal/schedule/manager_test.go` (`validateHours`'s full branch set — missing/malformed/backwards open-close, every `invalid_break` case including break-outside-range at both ends — plus `Manager.Replace`'s own row-count/weekday-range/duplicate-weekday checks, calling with a nil-backed `Manager{}` since these all return before touching `repo`), `internal/admin/handler_test.go` (`parseHHMM` valid boundary times and malformed inputs).

  Before adding integration scenarios, checked what already existed rather than assuming the gap — found `TestRBAC_WashingPointManagement`, `TestPhotos_UploadOwnershipAndCoverPromotion`, `TestSchedule_SeedingValidationAndOwnership`, `TestBoxes_CRUDOwnershipAndAvailabilityFilter`, and `TestAdmin_NetworkWideViews` already cover RBAC/ownership and the photo lifecycle and admin stats shape — so the connection-request approve/reject flow (`internal/connectionrequest`, phase 2, flagged as having "zero test coverage yet" in that phase's own log entry above) was the one genuine remaining gap, not the four-scenario set originally planned. Added `TestConnectionRequests_ApproveRejectLifecycle` to `scenarios_test.go`: staff forbidden (create+list, admin-only per `RegisterRoutes`), approving creates a `pending_review` washing point (found via `GET /admin/washing-points` by name, since `updateStatus`'s response only returns the connection request, not the point) with its default 7-row schedule and `boxes_count`-many boxes actually seeded (asserted by hitting the real public `GET .../schedule` and `GET .../boxes` endpoints, not by inspecting the DB directly), and both approve/reject correctly 409 `connection_request_already_reviewed` on a second review; rejecting leaves no washing point behind.

  All 3 new unit test files pass individually and as part of `make test` (full suite, no regressions). Docker was available this session — ran the new integration test alone first, then the full `make test-integration` suite (all pre-existing scenarios plus the new one): all green, ~66s total, zero leftover containers. `go vet ./...` and `go vet -tags=integration ./internal/integration/...` both clean.
